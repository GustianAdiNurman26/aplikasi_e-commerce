<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kelompok">

        <!-- 1. Halaman Keranjang (Anak dari MainActivity) -->
        <activity
            android:name=".AddedProductsActivity"
            android:exported="false"
            android:label="Keranjang Belanja"
            android:parentActivityName=".MainActivity">
            <!-- Meta-data tambahan untuk mendukung Android versi lama -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

        <!-- 2. Halaman Utama (Katalog) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Katalog Produk">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>